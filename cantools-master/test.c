#include <stdio.h>
// #include "./src/libcanblf/blfapi.h"
int
main()
{
	// FILE* fp = fopen("test.blf", "r");
	// BLFHANDLE h = blfCreateFile(fp);
	
	// printf("Hi %d ", h->magic);

	fclose(stdin);
	return 0;
}
